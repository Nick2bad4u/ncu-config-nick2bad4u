# Changelog

## Unreleased

- Convert the repository template into a publishable shared npm-check-updates config package.
- Add separate presets for standard npm projects and npm workspaces.
- Add typed config path/loading helpers and real NCU consumer smoke tests.
